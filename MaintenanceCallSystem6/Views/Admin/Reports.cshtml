@* @model IEnumerable<MaintenanceCallSystem6.ViewModels.TechnicianReportViewModel>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <h2>Technician Reports</h2>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Technician</th>
                <th>Call Description</th>
                <th>Date Logged</th>
                <th>Date Attended</th>
                <th>Time Taken (Days)</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var technician in Model)
            {
                foreach (var call in technician.Calls)
                {
                    <tr>
                        <td>@technician.TechnicianName</td>
                        <td>@call.Description</td>
                        <td>@call.DateLogged.ToString("yyyy/MM/dd")</td>
                        <td>@call.DateAttended.ToString("yyyy/MM/dd")</td>
                        <td>@call.TimeTaken.TotalDays</td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

 *@